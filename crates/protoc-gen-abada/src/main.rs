//! protoc / buf plugin: reads a `CodeGeneratorRequest` on stdin and writes a
//! `CodeGeneratorResponse` on stdout.

fn main() {
    eprintln!("protoc-gen-abada: not implemented yet (see docs/DESIGN.md)");
    std::process::exit(1);
}
